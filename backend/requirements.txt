# 智能证件照制作系统 - Python 依赖
# 使用 == 锁定精确版本，确保不同环境行为一致

# Web 框架
flask==3.1.0
flask-cors==5.0.1
gunicorn==23.0.0

# 图像处理（headless 版本，无 GUI 依赖，适合 Docker）
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
reportlab==4.2.2

# AI 人脸检测 / 人像分割
mediapipe==0.10.18

# 用户认证
PyJWT==2.10.1
bcrypt==4.2.1

# 测试
pytest==8.3.4
pytest-cov==6.0.0
